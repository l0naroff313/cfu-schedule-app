<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="UniversitySchedule.Mobile.Pages.ProfilePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Shell.NavBarIsVisible="False"
    Title="Профиль">
    <VerticalStackLayout Padding="16,24" Spacing="16">
        <Label Style="{StaticResource PageTitle}" Text="Профиль" />
        <Border Style="{StaticResource Card}">
            <VerticalStackLayout Spacing="8">
                <Label Style="{StaticResource SectionTitle}" Text="КФУ им. В. И. Вернадского" />
                <Label x:Name="InstituteLabel" Style="{StaticResource SecondaryText}" Text="Учебный профиль пока не настроен." />
                <Label x:Name="DirectionLabel" />
                <Label x:Name="GroupLabel" Style="{StaticResource SectionTitle}" />
                <Label x:Name="SyncLabel" Style="{StaticResource SecondaryText}" />
            </VerticalStackLayout>
        </Border>
        <Button Clicked="OnChangeProfileClicked" Text="Изменить учебную группу" />
    </VerticalStackLayout>
</ContentPage>
